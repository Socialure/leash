services:
  - type: web
    name: leash
    runtime: node
    region: ohio
    rootDir: app
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
